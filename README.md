# flight-simulator
A fully reactive backend service built with **Spring Boot 3** + **Spring WebFlux** that simulates a commercial fligh, exposing real-time metrics via a REST API and a Server-Sent Events (SSE) stream.
